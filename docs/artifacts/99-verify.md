# 99 — 최종 검증

> 이 문서의 모든 판정은 **Penpot을 직접 재조회하고 PNG를 직접 뽑아 판독한 것**만 근거로 삼는다.
> ⑥·⑦의 주장은 근거로 인정하지 않았고, 실제로 **⑥·⑦ 보고와 어긋난 수치 3건을 발견해 정정**했다.
> **읽기 전용 `3-toss`는 `penpot.currentFile.pages` 순회로만 읽었다. 전환하지 않았고 쓰지 않았다.**
> **이 단계에서 Penpot을 수정한 것은 0건이다.**

## 판정

| 항목 | 결과 |
|---|---|
| **전체** | **PASS** |
| 검증 Page | `3-toss-result` (id `cf8cfb09-3416-4c32-a2eb-38e95e5c2be9`) |
| 검증 시각 | 2026-08-02 |
| 직접 판독한 PNG | **14** (14개 화면 전부 + 기존 `홈` 대조 1장) |
| FAIL | **0** |
| WARN | **4** (전부 제출 저해 없음) |

---

## 검증 1 — Page 실체

| 항목 | 값 | 판정 |
|---|---|---|
| 최상위 board 총수 | **47** | PASS |
| └ `New/` 화면 | **14** | PASS |
| └ `TR /` 컴포넌트 선반 (`y < 0`) | **33** | PASS |
| 폭 390 준수 | **14 / 14** | **PASS** (PRD 7절 불변조건) |
| 빈 board (자식 0) | **0** | PASS |
| Auto Layout 적용 board | **861 / 879** (98.0%) | PASS |

### `New/` 화면 14개 (실측)

| # | 이름 | id (앞 12) | 크기 | 자식 | PNG |
|---|---|---|---|---|---|
| 1 | `New / Home` | `…6a5dd50cb32b` | 390 × 1754 | 24 | ✅ |
| 2 | `New / Results` | `…6a5f00dd8c6d` | 390 × **1305** | 25 | ✅ |
| 3 | `New / Results-Loading` | `…6a626350d375` | 390 × 841 | 15 | ✅ |
| 4 | `New / Results-Empty` | `…6a61de67e1fb` | 390 × 859 | 17 | ✅ |
| 5 | `New / Results-FilterSheet` | `…6a5fc6d8179c` | 390 × **1305** | 26 | ✅ |
| 6 | `New / Results-FareSheet` | `…6a5f73974723` | 390 × **1305** | 26 | ✅ |
| 7 | `New / Compare` | `…6a6022540327` | 390 × 986 | 15 | ✅ |
| 8 | `New / Compare-Empty` | `…6a6294130e8c` | 390 × 684 | 18 | ✅ |
| 9 | `New / FlightDetail` | `…6a607ad174b2` | 390 × 1123 | 14 | ✅ |
| 10 | `New / Watchlist` | `…6a60de69af01` | 390 × 974 | 21 | ✅ |
| 11 | `New / Watchlist-SoldOut` | `…6a62d97614f2` | 390 × 974 | 23 | ✅ |
| 12 | `New / DealCollection` | `…6a61350cc2d5` | 390 × 1495 | 25 | ✅ |
| 13 | `New / DestinationActivities` | `…6a618eecac6f` | 390 × 1235 | 18 | ✅ |
| 14 | `New / Home-FirstRun` | `…6a62086569bf` | 390 × 1689 | 29 | ✅ |

> **⑦ 보고 정정 (WARN-1)**: ⑦은 Results 계열 3화면을 **1361**이라 적었으나 실측은 **1305**다(−56).
> ⑥은 같은 화면을 **1560**이라 적었다. 세 문서의 수치가 전부 다르다.
> **PNG 판독 결과 1305에서 잘림·유령 공백이 없어 실해는 없다.** 문서 수치만 stale하다.
>
> Penpot은 `New/Home`을 `path="New"` + `name="Home"`으로 쪼개 저장하므로 조회 결과가
> `New / Home`으로 보인다. **PRD 7절 `New/` 접두 요건은 충족**한다.

---

## 검증 2 — PRD 필수 요건 (PNG 직접 판독)

### 2-1. PRD 5-1 `New/Home` 필수 요소 5종

| PRD 요구 | PNG에서 실제로 본 것 | 판정 |
|---|---|---|
| 항공권 검색 진입 — 출발지·목적지·날짜·인원 | `서울(SEL) ⇄ 도쿄(TYO)` / `8월 12일 – 8월 16일` / `성인 1명 · 일반석` + 전체폭 `항공권 검색` 버튼 | **PASS** |
| 예정된 여정 — 남은 기간 + 투어·체험·입장권 | `D-9` `도쿄 나리타` `NH862` + 입장권·1일권·미식 투어 3건(사진·평점·가격) | **PASS** |
| 둘러보기 묶음 — 여러 갈래 | 갈래 칩 **4개**(마감 임박 / 할인율 / 테마 / 땡처리) + DealCard 4장 | **PASS** |
| 담아둔 목적지 | `담아둔 목적지 3` + 후쿠오카·치앙마이·삿포로 + ▼12,000 / ▼4,000 / ▲8,000 + 알림 문구 | **PASS** |
| 하단 주요 이동 수단 | 5탭 TabBar(홈·검색·담아둠·내 여정·전체), **아이콘 전부 렌더**(placeholder 아님) | **PASS** |

### 2-2. PRD 5-2 `New/Results` 필수 요소 6종

| PRD 요구 | PNG에서 실제로 본 것 | 판정 |
|---|---|---|
| 항공편 목록 — 출도착시각·소요·경유·항공사·가격 | 12행. 예: `KE 대한항공 KE703 / 09:20 → 11:45 / 2시간 25분 · 직항 / ₩232,000` — **5요소 전부** | **PASS** |
| 경유 구간 표현 — 어디서 얼마나 | `상하이 푸둥에서 5시간 20분 대기` · `홍콩에서 6시간 05분` · `베이징에서 6시간 40분` · `마닐라에서 5시간 55분` | **PASS** |
| 출발일 앞뒤 가격 비교 | 날짜칩 8/9~8/13, 8/12 선택(파랑) · 8/13 최저(초록) 색 구분 | **PASS** |
| 조건 좁히기 | 진입점 칩(`조건 좁히기`·직항만·오전 출발·항공사·가격순) + FilterSheet(경유/시간대/항공사/가격 4군) | **PASS** |
| 비교함 | 하단 `비교함 2/3` + `나란히 비교` 버튼 → `New/Compare` 7행 비교표 | **PASS** |
| **운임 등급 선택** | FareSheet — 스탠다드/플렉스/프리미엄 플렉스 3종, 환불수수료·날짜변경·수하물 조건 상이 | **PASS** |

### 2-3. 🔴 PRD 5-2 명시 제약 — "별도 화면이 아니라 이 화면 위에 올라오는 형태"

| 검증 방법 | 결과 |
|---|---|
| FareSheet PNG에서 **뒤 항공편 목록이 비쳐 보이는가** | **✅ 보인다.** KE·OZ·LJ·MU·MM 5행 + 날짜칩 + 필터칩 + TopBar가 시트 뒤로 흐리게 관통. 상단 grabber 핸들도 확인 |
| 독립 최상위 화면인가 | **아니다.** `New/Results` clone 위 오버레이. base와 동일 1305 높이 |
| 스크림 사각형 사용 | **0개.** 뒤 보드 `opacity` 방식 (AGENTS.md 함정 회피) |
| FilterSheet도 동일 방식인가 | ✅ 동일 — 목록·칩 전부 관통 확인 |

> **PRD가 명시적으로 건 유일한 구조 제약이며, 통과했다.**

### 2-4. PRD 6절 — 기존 톤앤매너 유지

기존 `3-toss` Page의 `홈` board를 **읽기 전용으로 export해 나란히 대조**했다.

| 항목 | 기존 `홈` | 신규 `New/Home` | 판정 |
|---|---|---|---|
| 배경 | 다크(near-black) | 동일 다크 | ✅ |
| 카드 | 라운드 대형 카드 그룹핑 | 동일 | ✅ |
| 강조색 | 토스 블루 | 동일 계열 블루 | ✅ |
| 상승/하락 | 초록/빨강 | 동일 (▼초록 ▲빨강) | ✅ |
| 하단 탭 | 5탭 아이콘+라벨 | 동일 5탭 구조 | ✅ |
| 타이포 | 볼드 제목 + 회색 보조 | 동일 위계 | ✅ |

**"다른 제품처럼 보이면 안 된다"는 조건 충족.** 도메인만 금융 → 여행으로 바뀌었고 시각 언어는 연속적이다.

### 2-5. PRD 4절 사용자 상황 커버리지 (PNG 확인)

| PRD 4절 문장 | 화면 | PNG 근거 | 판정 |
|---|---|---|---|
| 40~60건 / 성수기 200건 | Results | `총 52건` | ✅ |
| 비교함 최대 3개 | Results·Compare·Compare-Empty | `2/3` · `1자리 남음 · 최대 3개까지` · `0/3` | ✅ |
| 예정 여정 D-14부터 | Home·Home-FirstRun | `D-9` / `출발 14일 전부터 여기에 표시돼요` | ✅ |
| 첫 실행 = 여정도 담아둔 것도 0 | Home-FirstRun | 두 섹션 **모두** 빈 상태, 검색·둘러보기는 유지 | ✅ |
| 조건 좁히면 0건 | Results-Empty | `총 0건` + 탈출 3경로(18/31/47건) + 초기화 | ✅ |
| 실시간 요금 수 초 소요 | Results-Loading | `52개 항공사 요금을 실시간으로 불러오는 중` + `보통 3~5초` + 게이지 + 스켈레톤 6행 | ✅ |
| 결제 전 마감 | Watchlist-SoldOut | 마감 모달 + 대체 2건 + 뒤 행 `마감됨` 잔존 | ✅ |
| 오늘 자정 마감 / 70% 할인 | Home·DealCollection | `오늘 자정 마감` `남은 시간 05:41:22` / `70% 할인` | ✅ |

**PRD 5절 필수 2화면을 넘어 4절 상황 8종을 전부 화면으로 환원했다. 미충족 0건.**

---

## 검증 3 — 라이브러리 재사용 (PRD 평가 항목)

| 항목 | 실측값 | 판정 |
|---|---|---|
| `TR /` 컴포넌트 선반 실재 | **33개** board, `y < 0` 영역 | PASS |
| 로컬 라이브러리 등록 컴포넌트 | **68개** (변형 포함) | PASS |
| **화면 내 루트 인스턴스 총계** | **368** | **PASS** |
| 무의미 이름(`Frame 27` 류) 정규식 매치 | **0건** | PASS |

### 화면별 인스턴스 실측 (복붙이 아님을 증명)

| 화면 | 루트 인스턴스 | 전체 노드 | 화면 | 루트 인스턴스 | 전체 노드 |
|---|---|---|---|---|---|
| `New/Home` | **25** | 282 | `New/Compare` | **15** | 99 |
| `New/Results` | **32** | 203 | `New/Compare-Empty` | **23** | 125 |
| `New/Results-Loading` | **38** | 221 | `New/FlightDetail` | **14** | 84 |
| `New/Results-Empty` | **37** | 209 | `New/Watchlist` | **13** | 133 |
| `New/Results-FilterSheet` | **45** | 253 | `New/Watchlist-SoldOut` | **17** | 151 |
| `New/Results-FareSheet` | **36** | 238 | `New/DealCollection` | **28** | 139 |
| `New/Home-FirstRun` | **28** | 164 | `New/DestinationActivities` | **17** | 97 |

**인스턴스 0인 화면 없음.** 전 화면이 인스턴스로 조립됐다. 복붙 흔적 없음 —
`isComponentInstance()`로 실판정했고, 중첩 인스턴스는 이중 계상하지 않았다.

---

## 검증 4 — 구조와 네이밍 (PRD 평가 항목)

| 항목 | 실측 | 판정 |
|---|---|---|
| 최상위 프레임 `New/` 접두 | 14 / 14 | PASS |
| `Frame 27` 류 무의미 이름 | **0건** (정규식 `^(Frame\|Rectangle\|Ellipse\|Group\|Board\|Path\|Text\|Layer)[\s\-_]?\d+$`, 전 노드 2,400+ 스캔) | **PASS** |
| 의미 기반 네이밍 | `FlightRow/Direct` · `StopoverBar` · `CompareTray` · `PriceDayCell/Lowest` · `WatchRow` | PASS |
| Auto Layout | **861 / 879 board (98.0%)** 가 flex/grid 보유 | PASS |
| 화면 최상위 flex | 14 / 14 전부 `flex:column` | PASS |

> PRD가 `Frame 27`을 명시적 감점 사유로 지목했는데 **0건**이다.

---

## 검증 5 — 산출물

| 파일 | 존재 | 줄수 | 표 행수 | 내용 채워짐 | 판정 |
|---|---|---|---|---|---|
| `01-prd-spec.md` | ✅ | 190 | 87 | ✅ | PASS |
| `02-design-audit.md` | ✅ | 397 | 220 | ✅ | PASS |
| `03-screen-list.md` | ✅ | 581 | 297 | ✅ | PASS |
| `04-tokens.md` | ✅ | 523 | 183 | ✅ | PASS |
| `05-components.md` | ✅ | 378 | 161 | ✅ | PASS |
| `06-screens.md` | ✅ | 296 | 141 | ✅ | PASS |
| `07-qa.md` | ✅ | 225 | 95 | ✅ | PASS |

부가: `00-env-facts.md`(169줄) — 환경 함정 사전. 표만 있고 비어 있는 파일 **0건**.

> `_stale-stock-run/`은 다른 PRD(증권)의 폐기 산출물이라 지침대로 읽지 않았다.
> **다만 제출 전 삭제하거나 `.gitignore` 처리하는 편이 낫다 (WARN-4).**

---

## 검증 6 — QA BLOCK

| 항목 | 건수 | 판정 |
|---|---|---|
| `07-qa.md` E절 BLOCK | **0** | **PASS** |
| MAJOR | 1건 발견 → **⑦이 수정 완료** | PASS |
| MINOR 잔여 | 3건 | WARN |

**MAJOR 수정을 PNG로 독립 재확인**: `New/Compare`의 `환불` 행에서 `수수료 ₩120,000`이
**중립 회색**으로 렌더된다. 초록은 `가격`·`소요 시간`·`경유` 3행에만 남아 있다.
"환불 수수료 12만원이 좋은 조건처럼 읽히던" 결함은 **실제로 해소됐다**.

### MINOR 잔여 3건 재판정 (PNG 직접 확인)

| ⑦ 번호 | 내용 | 이번 PNG 판독 | 재판정 |
|---|---|---|---|
| #2 | `FlightRow/Stopover`의 Meta 2줄 줄바꿈 | **재현되지 않는다.** `11시간 40분 · 경유` 1줄, 행 높이가 직항 행과 일치 | **해소됨** |
| #3 | `Compare-Empty` HowTo 번호 우측 정렬 | **재현된다.** `1 2 3`이 우측 끝에 붙어 순서가 아닌 수치처럼 읽힘 | **WARN 유지** |
| #4 | `Compare` 계열 TopBar 우측 아이콘 부재 | **재현된다.** 단 `Compare`에는 `전체 해제`가 텍스트로 있어 기능 공백은 아님 | **WARN (의도 가능)** |

---

## 검증 7 — 재현성 / 반하드코딩 (하네스 채점 축)

`.claude/agents/` · `.claude/skills/` 전체에 대해 이번 PRD 고유명사를 grep했다.

검색어: `3-toss` · `도쿄` · `서울` · `오사카` · `타이베이` · `방콕` · `다낭` · `후쿠오카` ·
`치앙마이` · `삿포로` · `대한항공` · `아시아나` · `진에어` · `항공권` · `toss` · `트립하다` ·
`New/Home` · `New/Results` · 팀원 이름

| 대상 | 발견된 고유명사 | 판정 |
|---|---|---|
| `.claude/agents/stage-*.md` | **0건** | **PASS** |
| `.claude/skills/**` | **0건** | **PASS** |

**agent 지침에 제품 고유명사도, 고정된 화면 개수도 박혀 있지 않다.**
Page 이름은 인자로 받고, 화면 목록은 ③이 PRD에서 계산한다.
**미공개 심사 PRD로 교체해도 무너지지 않는 구조다.**

---

## WARN 4건 (제출 저해 없음)

| # | 항목 | 내용 | 영향 |
|---|---|---|---|
| 1 | 문서 수치 stale | Results 계열 높이가 ⑥ 1560 / ⑦ 1361 / **실측 1305** 로 3자 불일치 | 문서 정확도만. **디자인 실해 없음** (PNG 잘림·공백 0) |
| 2 | `Compare-Empty` 번호 정렬 | `1 2 3` 우측 끝 배치 | 미세 가독성 |
| 3 | `Compare` 계열 TopBar 우측 아이콘 | 다른 12화면과 리듬 불일치 | 미세. 의도면 무시 가능 |
| 4 | `_stale-stock-run/` 잔존 | 다른 PRD 폐기 산출물이 `docs/artifacts/`에 남음 | 심사자 혼동 소지. 삭제 권장 |

---

## 실패 항목 → 되돌릴 단계

| 실패 | 담당 단계 | 무엇을 다시 해야 하나 |
|---|---|---|
| **없음** | — | **되돌릴 단계 없음** |

WARN 4건은 전부 선택적 개선이다. 되돌림이 필요한 FAIL은 발생하지 않았다.

---

## 결론

> **제출 가능하다.**
>
> `3-toss-result`에 폭 390의 `New/` 화면 **14개**가 실재하고, PRD 5-1·5-2 필수 요소
> **11종 전부**와 PRD가 건 유일한 구조 제약(운임 등급 = Results 위 오버레이)을
> **PNG 직접 판독으로 확인**했다. 기존 다크 톤앤매너는 연속적이고, 화면은 **368개
> 컴포넌트 인스턴스**로 조립됐으며 무의미 이름은 **0건**, Auto Layout은 **98%** 적용됐다.
> QA BLOCK **0건**, agent 지침의 PRD 고유명사 **0건**으로 재현성도 확보됐다.
